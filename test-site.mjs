import fs from "node:fs";import vm from "node:vm";const root=new URL("./",import.meta.url);for(const f of ["assets/js/common.js","assets/js/home.js","assets/js/tools-data.js","assets/js/tool-engine.js"]){new vm.Script(fs.readFileSync(new URL(f,root),"utf8"),{filename:f})}const data=fs.readFileSync(new URL("assets/js/tools-data.js",root),"utf8");const box={window:{}};vm.runInNewContext(data,box);const tools=box.window.TOOLBOX_TOOLS;if(tools.length!==84)throw Error("Expected 84 tools");const home=fs.readFileSync(new URL("index.html",root),"utf8");const cards=(home.match(/class="tool-card"/g)||[]).length;if(cards!==84)throw Error(`Expected 84 static home cards, found ${cards}`);for(const t of tools){const url=new URL(`tools/${t.slug}/index.html`,root);if(!fs.existsSync(url))throw Error(`Missing ${t.slug}`);const page=fs.readFileSync(url,"utf8");if(!page.includes(`data-tool="${t.slug}"`))throw Error(`Missing data-tool for ${t.slug}`);if(!page.includes(`How to use the ${t.title}`))throw Error(`Missing guide for ${t.slug}`);if(!page.includes("Donate with PayPal"))throw Error(`Missing donation link for ${t.slug}`)}for(const asset of ["assets/logo.png","assets/logo-192.png","assets/logo-512.png","assets/favicon.png"]){if(!fs.existsSync(new URL(asset,root)))throw Error(`Missing ${asset}`)}console.log(`Validated ${tools.length} tools, ${cards} homepage cards, tool initialisation attributes, guides and brand assets.`);