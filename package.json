{
  "name": "open-browser-toolbox-tests",
  "private": true,
  "version": "5.1.2",
  "description": "Automated tests for the static OpenToolbox website, including scoped navigation, dark-mode and full catalogue regression checks.",
  "type": "module",
  "scripts": {
    "test:static": "node test-site.mjs",
    "test:browser": "playwright test",
    "test": "npm run test:static && npm run test:browser"
  },
  "devDependencies": {
    "@playwright/test": "1.55.0"
  }
}
