{
  "name": "open-browser-toolbox-tests",
  "private": true,
  "version": "5.2.0",
  "description": "Automated tests for OpenToolbox v5.2.0, including visible spacing, desktop-only full navigation, mobile menu behaviour and full catalogue regression checks.",
  "type": "module",
  "scripts": {
    "test:static": "node test-site.mjs",
    "test:browser": "playwright test",
    "test": "npm run test:static && npm run test:browser"
  },
  "devDependencies": {
    "@playwright/test": "1.55.0"
  }
}
